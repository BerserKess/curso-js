/**
 * Quando se trabalha com tipos primitivos se realiza uma troca por valor
 * Quando é por referência se realiza uma troca por referência
 * Usar nulo quando se quer zerar o valor de uma variavel
 */

let valor; // foi declarada mas não inicializada (undefined)
console.log(valor);

valor = null; // ela não está apontando para nenhum endereço da memória (ausencia de valor)
console.log(valor)