/**
 * Fix the code in the code tab to pass this
 * challenge (only syntax errors).
 * Look at the examples below to get an idea
 * of what the function should do.
 * READ EVERY WORD CAREFULLY, CHARACTER BY CHARACTER!
 * Don't overthink this challenge;
 * it's not supposed to be hard.
 */

function cubes(a) {
	return console.log(a ** 3); // palavrar return errada!
}

cubes(3);